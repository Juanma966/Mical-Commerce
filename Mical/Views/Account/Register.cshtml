@using Microsoft.AspNetCore.Authentication
@model RegisterVm
@{
    ViewData["Title"] = "Crear cuenta";
    var returnUrl = ViewData["ReturnUrl"] as string;
    var externalLogins = ViewData["ExternalLogins"] as List<AuthenticationScheme> ?? new();
}

<section class="auth-section py-5" style="margin-top:100px;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="card shadow-sm border-0">
                    <div class="card-body p-4 p-md-5">
                        <h1 class="h3 mb-4 text-center">Crear cuenta</h1>

                        <form asp-action="Register" asp-route-returnUrl="@returnUrl" method="post" novalidate>
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                            <div class="mb-3">
                                <label asp-for="FullName" class="form-label"></label>
                                <input asp-for="FullName" class="form-control" autocomplete="name" />
                                <span asp-validation-for="FullName" class="text-danger small"></span>
                            </div>

                            <div class="mb-3">
                                <label asp-for="Email" class="form-label"></label>
                                <input asp-for="Email" class="form-control" autocomplete="email" />
                                <span asp-validation-for="Email" class="text-danger small"></span>
                            </div>

                            <div class="mb-3">
                                <label asp-for="Password" class="form-label"></label>
                                <input asp-for="Password" class="form-control" autocomplete="new-password" />
                                <span asp-validation-for="Password" class="text-danger small"></span>
                                <div class="form-text">Mínimo 8 caracteres, con mayúscula, minúscula y número.</div>
                            </div>

                            <div class="mb-4">
                                <label asp-for="ConfirmPassword" class="form-label"></label>
                                <input asp-for="ConfirmPassword" class="form-control" autocomplete="new-password" />
                                <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                            </div>

                            <button type="submit" class="btn btn-dark w-100 py-2">Crear cuenta</button>
                        </form>

                        @if (externalLogins.Count > 0)
                        {
                            <div class="text-center text-muted my-3">o</div>
                            @foreach (var scheme in externalLogins)
                            {
                                <form asp-action="ExternalLogin" asp-controller="Account" asp-route-returnUrl="@returnUrl" method="post">
                                    <input type="hidden" name="provider" value="@scheme.Name" />
                                    <button type="submit" class="btn btn-outline-secondary w-100 py-2">
                                        Continuar con @scheme.DisplayName
                                    </button>
                                </form>
                            }
                        }

                        <p class="text-center mt-4 mb-0">
                            ¿Ya tenés cuenta?
                            <a asp-action="Login" asp-route-returnUrl="@returnUrl">Iniciá sesión</a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
